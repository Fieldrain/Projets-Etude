// Metro.cpp : définit le point d'entrée pour l'application console.
//

#include "stdafx.h"


int main()
{
	/*switch (OS) {
	case 0:
		Window* fenetre = new Window();
		InterfaceHistoire* itH = new Interface_Combat(fenetre);
		InterfaceCombat* itC = new Interface_Combat(fenetre);
			break;
	default:
		cout << "Non Compatible ! Appuyer sur une touche pour fermer la console.";
		cin.get();
		exit(1);
		break;
	}*/
    return 0;
}

