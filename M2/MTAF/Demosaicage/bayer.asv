function [ Image_CFA ] = bayer( Input_RGB )
[m,n] = size(Input_RGB);

couleur = 2;
for i = 1:m
    if (mod(i,2) == 0)
        
    else
            
    end
    
    for j = 1:n
        
        if (
        
        Image_CFA(i,j)
    end
end
